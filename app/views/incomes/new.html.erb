<%= render layout: "shared/form_page", locals: {
  resource: @income,
  title: params[:duplicate_from].present? ? "Duplicar Receita" : "Nova Receita",
  eyebrow: "Receitas",
  description: params[:duplicate_from].present? ? "Revise os dados antes de salvar a nova receita." : "Registre valor, categoria e período da receita.",
  icon: "payments",
  back_path: incomes_path
} do %>
    <%= render "form", income: @income %>
<% end %>
