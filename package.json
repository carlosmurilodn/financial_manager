{
  "name": "app",
  "private": true,
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.18",
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.8",
    "esbuild-sass-plugin": "^3.3.1",
    "sass": "^1.93.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  },
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "esbuild app/javascript/application.js --bundle --sourcemap --outdir=app/assets/builds --format=esm --watch"
  }
}
